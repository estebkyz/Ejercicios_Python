distancia = float(input("Digite la distancia a escalar (metros): "))

tiempo_por_metro = 5 / 7

tiempo_total = distancia * tiempo_por_metro

print("Para escalar", distancia, "metros")
print("Se tarda:", tiempo_total, "horas")
